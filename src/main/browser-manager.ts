import {app,BrowserWindow,WebContentsView,session,shell} from 'electron';import {promises as fs} from 'node:fs';import path from 'node:path';import type {Account,BrowserBounds,LoginStatus,PublishDraft,TaskStatus} from '../shared/types';import {platformMap} from '../shared/platforms';type AccountUpdate={name?:string;loginStatus?:LoginStatus};type PublishProgress=(status:TaskStatus,message:string)=>Promise<void>;const sleep=(ms:number)=>new Promise(resolve=>setTimeout(resolve,ms));export class BrowserManager{private views=new Map<string,WebContentsView>();private active?:string;private bounds:BrowserBounds={x:280,y:160,width:900,height:650};constructor(private win:BrowserWindow,private updateAccount:(id:string,update:AccountUpdate)=>Promise<void>){}private ensureView(account:Account){let view=this.views.get(account.id);if(view)return view;view=new WebContentsView({webPreferences:{session:session.fromPartition('persist:account-'+account.id),nodeIntegration:false,contextIsolation:true,sandbox:true}});view.webContents.setWindowOpenHandler(({url})=>{if(url.startsWith('http'))view?.webContents.loadURL(url);else shell.openExternal(url);return{action:'deny'}});const inspect=()=>this.inspectAccount(account,view!);view.webContents.on('did-finish-load',inspect);view.webContents.on('did-navigate',inspect);view.webContents.on('page-title-updated',inspect);this.views.set(account.id,view);return view}async show(account:Account,target:'home'|'publish'='home'){const view=this.ensureView(account);if(this.active&&this.active!==account.id){const old=this.views.get(this.active);if(old)this.win.contentView.removeChildView(old)}if(!this.win.contentView.children.includes(view))this.win.contentView.addChildView(view);this.active=account.id;view.setBounds(this.bounds);const p=platformMap[account.platform];if(!view.webContents.getURL()||target==='publish')await view.webContents.loadURL(target==='publish'?p.publishUrl:p.homeUrl)}async autoPublish(account:Account,draft:PublishDraft,progress:PublishProgress){const videoPath=draft.mediaPaths[0];if(!videoPath)return{status:'failed' as const,message:'草稿没有选择视频文件'};const videoStat=await fs.stat(videoPath).catch(()=>undefined);if(!videoStat?.isFile()||videoStat.size<1024)return{status:'failed' as const,message:'视频文件不存在或为空：'+videoPath};const automationWindow=new BrowserWindow({show:false,skipTaskbar:true,width:1280,height:900,webPreferences:{session:session.fromPartition('persist:account-'+account.id),nodeIntegration:false,contextIsolation:true,sandbox:true,backgroundThrottling:false}});automationWindow.setOpacity(0);automationWindow.show();automationWindow.focus();const wc=automationWindow.webContents;wc.focus();try{await progress('opening','正在进入'+platformMap[account.platform].name+'发布页面');await wc.loadURL(platformMap[account.platform].publishUrl);await sleep(2500);const initialLogin=await this.getLoginState(account,wc);if(!initialLogin.loggedIn){await this.updateAccount(account.id,{loginStatus:'logged_out'});return{status:'manual_required' as const,message:platformMap[account.platform].name+'登录已失效，请在账号管理重新扫码登录'}}await this.updateAccount(account.id,{loginStatus:'logged_in',...(initialLogin.name?{name:initialLogin.name}:{})});if(account.platform==='bilibili'){const hasStaleDraft=await wc.executeJavaScript("/本地浏览器存在.*未提交/.test(document.body?.innerText||'')").catch(()=>false);if(hasStaleDraft){const discarded=await this.waitAndClickButton(wc,['不用了','放弃草稿','重新投稿'],12000,false);if(!discarded)throw new Error('Bilibili stale draft prompt could not be dismissed');await sleep(1800)}const existingParts=await wc.executeJavaScript("((document.body?.innerText||'').match(/\u4e0a\u4f20\u5b8c\u6210/g)||[]).length");if(existingParts){const batchOpened=await this.clickButtonByText(wc,['\u6279\u91cf\u64cd\u4f5c'],false);if(batchOpened){await sleep(700);await this.clickButtonByText(wc,['\u5168\u9009'],false);await sleep(500);const removed=await this.clickButtonByText(wc,['\u5220\u9664','\u79fb\u9664'],true);if(removed){await sleep(700);await this.clickButtonByText(wc,['\u786e\u5b9a','\u786e\u8ba4'],true);await sleep(1500)}}}}await progress('uploading','正在等待平台上传控件');let videoNodeId=0;if(account.platform==='bilibili'){const nearbyVideo=await this.waitForFileInputNearTexts(wc,['上传视频','点击上传或将视频拖拽到此区域'],60000);if(!nearbyVideo)throw new Error('Bilibili current video upload control not found');videoNodeId=nearbyVideo.nodeId;await this.setFileInput(wc,videoNodeId,[draft.mediaPaths[0]])}else{const inputs=await this.waitForFileInputs(wc,60000);if(!inputs.length&&account.platform==='douyin'){await this.chooseFileFromButton(wc,'douyin',draft.mediaPaths[0]);videoNodeId=-1}else{if(!inputs.length){const login=await this.getLoginState(account,wc);return{status:'manual_required' as const,message:login.loggedIn?'Video upload control not found':'Login required'}}const videoInput=inputs.find(x=>/video/i.test(x.accept))||inputs.find(x=>!/image/i.test(x.accept))||inputs[0];if(!videoInput)throw new Error('Video upload control not found');videoNodeId=videoInput.nodeId;await this.setFileInput(wc,videoNodeId,[draft.mediaPaths[0]])}}await progress('uploading','\u6b63\u5728\u4e0a\u4f20\u89c6\u9891');const editorReady=await this.waitForEditor(wc,account.platform,120000);if(!editorReady)throw new Error('视频已选择，但未进入平台编辑页面');if(account.platform==='bilibili'){const uploadStart=Date.now();let uploadFinished=false;while(Date.now()-uploadStart<240000){const uploadState=await wc.executeJavaScript("(()=>{const text=document.body?.innerText||'';const completed=/上传完成/.test(text);const uploading=/上传中|剩余时间|当前速度|已上传/.test(text)&&!completed;return{completed,uploading,text:text.slice(0,5000)}})()");if(uploadState.completed&&!uploadState.uploading){uploadFinished=true;break}await sleep(1500)}if(!uploadFinished)throw new Error('B站视频上传未在等待时间内完成')}if(account.platform!=='douyin')await this.fillContent(wc,draft,account.platform);if(account.platform==='bilibili'){if(!wc.debugger.isAttached())wc.debugger.attach('1.3');await wc.debugger.sendCommand('DOM.enable');const flat=await wc.debugger.sendCommand('DOM.getFlattenedDocument',{depth:-1,pierce:true});const textareaCandidates=[] as Array<{backendNodeId:number;area:number}>;for(const node of flat.nodes as Array<{nodeId:number;backendNodeId?:number;nodeName:string}>){if(node.nodeName!=='TEXTAREA')continue;const backendNodeId=node.backendNodeId||node.nodeId;try{const model=await wc.debugger.sendCommand('DOM.getBoxModel',{backendNodeId});const quad=(model.model.border||model.model.content) as number[];const area=(Math.max(quad[0],quad[2],quad[4],quad[6])-Math.min(quad[0],quad[2],quad[4],quad[6]))*(Math.max(quad[1],quad[3],quad[5],quad[7])-Math.min(quad[1],quad[3],quad[5],quad[7]));if(area>100)textareaCandidates.push({backendNodeId,area})}catch{}}textareaCandidates.sort((a,b)=>b.area-a.area);const textareaNode=textareaCandidates[0];if(textareaNode){const resolved=await wc.debugger.sendCommand('DOM.resolveNode',{backendNodeId:textareaNode.backendNodeId});const objectId=resolved.object?.objectId;if(objectId){await wc.debugger.sendCommand('Runtime.callFunctionOn',{objectId,functionDeclaration:'function(value){const setter=Object.getOwnPropertyDescriptor(HTMLTextAreaElement.prototype,"value")?.set;setter?setter.call(this,value):this.value=value;this.dispatchEvent(new InputEvent("input",{bubbles:true,inputType:"insertText",data:value}));this.dispatchEvent(new Event("change",{bubbles:true}));this.dispatchEvent(new Event("blur",{bubbles:true}));return this.value}',arguments:[{value:draft.description}],returnByValue:true});await sleep(800)}}const declarationPlaceholder='\u8bf7\u9009\u62e9\u7b26\u5408\u60a8\u89c6\u9891\u5185\u5bb9\u7684\u521b\u4f5c\u58f0\u660e';const declarationDom=await wc.debugger.sendCommand('DOM.getFlattenedDocument',{depth:-1,pierce:true});const declarationAll=declarationDom.nodes as Array<{nodeId:number;backendNodeId?:number;parentId?:number;nodeName:string;attributes?:string[]}>;const declarationMap=new Map(declarationAll.map(node=>[node.nodeId,node]));const declarationInputs=[] as Array<{nodeId:number;backendNodeId:number;x:number;y:number;area:number}>;for(const node of declarationAll.filter(node=>node.nodeName==='INPUT')){const attrs=node.attributes||[];const values=new Map<string,string>();for(let index=0;index<attrs.length;index+=2)values.set(attrs[index],attrs[index+1]||'');if(values.get('placeholder')!==declarationPlaceholder)continue;let hidden=false,current:typeof node|undefined=node;for(let depth=0;current&&depth<8;depth++,current=current.parentId?declarationMap.get(current.parentId):undefined){const joined=(current.attributes||[]).join(' ');if(/display\s*:\s*none|visibility\s*:\s*hidden/i.test(joined))hidden=true}if(hidden)continue;try{const model=await wc.debugger.sendCommand('DOM.getBoxModel',{nodeId:node.nodeId});const quad=(model.model.border||model.model.content) as number[];const xs=[quad[0],quad[2],quad[4],quad[6]],ys=[quad[1],quad[3],quad[5],quad[7]],width=Math.max(...xs)-Math.min(...xs),height=Math.max(...ys)-Math.min(...ys);if(width>10&&height>10)declarationInputs.push({nodeId:node.nodeId,backendNodeId:node.backendNodeId||node.nodeId,x:xs.reduce((sum,value)=>sum+value,0)/4,y:ys.reduce((sum,value)=>sum+value,0)/4,area:width*height})}catch{}}declarationInputs.sort((a,b)=>b.area-a.area);const declarationInput=declarationInputs[0];if(!declarationInput)throw new Error('Bilibili visible declaration input not found');const declarationResolvedBefore=await wc.debugger.sendCommand('DOM.resolveNode',{backendNodeId:declarationInput.backendNodeId});if(declarationResolvedBefore.object?.objectId)await wc.debugger.sendCommand('Runtime.callFunctionOn',{objectId:declarationResolvedBefore.object.objectId,functionDeclaration:'function(){this.scrollIntoView({block:"center",inline:"nearest"});return true}',returnByValue:true});await sleep(800);const declarationModel=await wc.debugger.sendCommand('DOM.getBoxModel',{backendNodeId:declarationInput.backendNodeId});const declarationQuad=(declarationModel.model.border||declarationModel.model.content) as number[];const declarationXs=[declarationQuad[0],declarationQuad[2],declarationQuad[4],declarationQuad[6]],declarationYs=[declarationQuad[1],declarationQuad[3],declarationQuad[5],declarationQuad[7]];const declarationX=declarationXs.reduce((sum,value)=>sum+value,0)/4,declarationY=declarationYs.reduce((sum,value)=>sum+value,0)/4;await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mouseMoved',x:declarationX,y:declarationY});await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mousePressed',x:declarationX,y:declarationY,button:'left',buttons:1,clickCount:1});await sleep(100);await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mouseReleased',x:declarationX,y:declarationY,button:'left',buttons:0,clickCount:1});await sleep(900);const declarationClick=await this.clickSmallestByText(wc,'\u542bAI\u751f\u6210\u5185\u5bb9');await sleep(900);const declarationResolved=await wc.debugger.sendCommand('DOM.resolveNode',{backendNodeId:declarationInput.backendNodeId});let declarationValue='';if(declarationResolved.object?.objectId){const valueResult=await wc.debugger.sendCommand('Runtime.callFunctionOn',{objectId:declarationResolved.object.objectId,functionDeclaration:'function(){return this.value||""}',returnByValue:true});declarationValue=valueResult.result?.value||''}if(!declarationClick||!declarationValue)throw new Error('Bilibili declaration selection failed: '+JSON.stringify({declarationClick,declarationValue,declarationInput:{x:declarationX,y:declarationY}}));wc.sendInputEvent({type:'mouseMove',x:680,y:785});wc.sendInputEvent({type:'mouseDown',x:680,y:785,button:'left',clickCount:1});await sleep(100);wc.sendInputEvent({type:'mouseUp',x:680,y:785,button:'left',clickCount:1});await sleep(200);await wc.debugger.sendCommand('Input.insertText',{text:draft.description});await sleep(800);if(draft.coverPath){const coverUploaded=await this.uploadFileByActions(wc,draft.coverPath,'image',['添加主封面','封面设置','更换封面','上传封面','本地上传','点击上传'],[videoNodeId],45000);if(!coverUploaded)throw new Error('Bilibili cover upload control not found after retries');await sleep(2200);await this.waitAndClickButton(wc,['确定','保存','完成','应用'],15000,true);await sleep(1000)}}if(account.platform==='douyin'){await sleep(1200);if(!wc.debugger.isAttached())wc.debugger.attach('1.3');await wc.debugger.sendCommand('DOM.enable');const douyinFlat=await wc.debugger.sendCommand('DOM.getFlattenedDocument',{depth:-1,pierce:true});const douyinNodes=douyinFlat.nodes as Array<{nodeId:number;backendNodeId?:number;nodeName:string;attributes?:string[]}>;const readAttrs=(node:typeof douyinNodes[number])=>{const result=new Map<string,string>(),attrs=node.attributes||[];for(let index=0;index<attrs.length;index+=2)result.set(attrs[index],attrs[index+1]||'');return result};const visibleArea=async(node:typeof douyinNodes[number])=>{try{const backendNodeId=node.backendNodeId||node.nodeId;const model=await wc.debugger.sendCommand('DOM.getBoxModel',{backendNodeId});const quad=(model.model.border||model.model.content) as number[];return{backendNodeId,area:(Math.max(quad[0],quad[2],quad[4],quad[6])-Math.min(quad[0],quad[2],quad[4],quad[6]))*(Math.max(quad[1],quad[3],quad[5],quad[7])-Math.min(quad[1],quad[3],quad[5],quad[7]))}}catch{return undefined}};const titleCandidates=[] as Array<{backendNodeId:number;area:number}>;const editorCandidates=[] as Array<{backendNodeId:number;area:number}>;for(const node of douyinNodes){const attrs=readAttrs(node);const geometry=await visibleArea(node);if(!geometry||geometry.area<100)continue;if(node.nodeName==='INPUT'&&/\u4f5c\u54c1\u6807\u9898/.test(attrs.get('placeholder')||''))titleCandidates.push(geometry);if(attrs.get('contenteditable')==='true')editorCandidates.push(geometry)}titleCandidates.sort((a,b)=>b.area-a.area);editorCandidates.sort((a,b)=>b.area-a.area);const typeInto=async(backendNodeId:number,text:string)=>{const resolved=await wc.debugger.sendCommand('DOM.resolveNode',{backendNodeId});const objectId=resolved.object?.objectId;if(objectId)await wc.debugger.sendCommand('Runtime.callFunctionOn',{objectId,functionDeclaration:'function(){if(this instanceof HTMLInputElement||this instanceof HTMLTextAreaElement){const prototype=this instanceof HTMLInputElement?HTMLInputElement.prototype:HTMLTextAreaElement.prototype;const setter=Object.getOwnPropertyDescriptor(prototype,"value")?.set;setter?setter.call(this,""):this.value=""}else{this.innerHTML="";this.textContent=""}this.dispatchEvent(new InputEvent("input",{bubbles:true,inputType:"deleteContentBackward"}));this.dispatchEvent(new Event("change",{bubbles:true}));return true}',returnByValue:true});await wc.debugger.sendCommand('DOM.focus',{backendNodeId});await wc.debugger.sendCommand('Input.dispatchKeyEvent',{type:'rawKeyDown',key:'a',code:'KeyA',windowsVirtualKeyCode:65,modifiers:2});await wc.debugger.sendCommand('Input.dispatchKeyEvent',{type:'rawKeyDown',key:'Backspace',code:'Backspace',windowsVirtualKeyCode:8});await wc.debugger.sendCommand('Input.dispatchKeyEvent',{type:'keyUp',key:'Backspace',code:'Backspace',windowsVirtualKeyCode:8});await wc.debugger.sendCommand('Input.dispatchKeyEvent',{type:'keyUp',key:'a',code:'KeyA',windowsVirtualKeyCode:65,modifiers:2});await wc.debugger.sendCommand('Input.insertText',{text});await wc.debugger.sendCommand('Input.dispatchKeyEvent',{type:'keyDown',key:'Tab',code:'Tab',windowsVirtualKeyCode:9});await wc.debugger.sendCommand('Input.dispatchKeyEvent',{type:'keyUp',key:'Tab',code:'Tab',windowsVirtualKeyCode:9});await sleep(700)};if(!titleCandidates[0]||!editorCandidates[0])throw new Error('未找到抖音真实标题或简介控件');const douyinExpectedTitle=Array.from(draft.title).slice(0,30).join('');await typeInto(titleCandidates[0].backendNodeId,douyinExpectedTitle);await typeInto(editorCandidates[0].backendNodeId,draft.description);const douyinFilled=await wc.executeJavaScript("(()=>{const title=[...document.querySelectorAll('input')].find(e=>/作品标题/.test(e.placeholder||''));const editor=[...document.querySelectorAll('[contenteditable=true]')].find(e=>e.offsetParent!==null);return{title:title?.value||'',description:editor?.textContent||''}})()");if(douyinFilled.title!==douyinExpectedTitle||!douyinFilled.description.includes(draft.description))throw new Error('抖音真实输入未生效：'+JSON.stringify({...douyinFilled,expectedTitle:douyinExpectedTitle}));const size=await wc.executeJavaScript("(()=>({width:innerWidth,height:innerHeight}))()");const x=Math.round(size.width*0.525),y=Math.round(size.height*0.655);wc.sendInputEvent({type:'mouseMove',x,y});wc.sendInputEvent({type:'mouseDown',x,y,button:'left',clickCount:1});await sleep(100);wc.sendInputEvent({type:'mouseUp',x,y,button:'left',clickCount:1});await sleep(2000)}if(account.platform==='toutiao'){await this.clickButtonByText(wc,['我知道了','知道了']);await sleep(800)}if(account.platform==='toutiao'){await progress('uploading','头条视频上传中，等待上传完成');const toutiaoVideoReady=await this.waitForPublishReady(wc,'toutiao',600000);if(!toutiaoVideoReady)throw new Error('头条视频上传完成超时')}if(draft.coverPath&&(account.platform==='douyin'||account.platform==='toutiao')){if(account.platform==='toutiao'){const coverModalOpened=await this.clickButtonByText(wc,['上传封面'],false,true);if(!coverModalOpened)throw new Error('未找到头条上传封面入口');await sleep(1200);const localUploadOpened=await this.clickButtonByText(wc,['本地上传'],false,true);if(!localUploadOpened)throw new Error('未找到头条本地上传封面页签');const localCoverInput=await this.waitForMatchingFileInput(wc,'image',15000,[videoNodeId]);if(localCoverInput)await this.setFileInput(wc,localCoverInput.nodeId,[draft.coverPath]);else await this.chooseFileFromButton(wc,['本地上传','点击上传','上传图片'],draft.coverPath);await sleep(1800);await this.clickButtonByText(wc,['下一步'],true,true);await sleep(1000);const editorConfirmed=await this.clickButtonByText(wc,['裁剪完成','完成','确定','保存'],true,true);if(!editorConfirmed)throw new Error('未找到头条封面编辑确认按钮');await sleep(1000);const finalConfirmVisible=await wc.executeJavaScript("/完成后无法继续编辑/.test(document.body?.innerText||'')").catch(()=>false);if(finalConfirmVisible){const finalConfirmed=await this.clickButtonByText(wc,['确定'],false,true);if(!finalConfirmed)throw new Error('未找到头条封面二次确认按钮')}const coverDeadline=Date.now()+30000;let coverReady=false;while(Date.now()<coverDeadline){await sleep(1000);const confirmStillVisible=await wc.executeJavaScript("/完成后无法继续编辑/.test(document.body?.innerText||'')").catch(()=>false);if(confirmStillVisible)await this.clickButtonByText(wc,['确定'],false,true);coverReady=await wc.executeJavaScript("(()=>{const visible=e=>!!e&&e.offsetParent!==null;const pageText=document.body?.innerText||'';if(/封面编辑|完成后无法继续编辑/.test(pageText))return false;const labels=[...document.querySelectorAll('div,span,label')].filter(e=>visible(e)&&(e.textContent||'').trim()==='封面');for(const label of labels){let root=label.parentElement;for(let depth=0;root&&depth<7;depth++,root=root.parentElement){const text=(root.textContent||'').trim();if(!/清晰美观的封面|上传封面/.test(text))continue;const visual=[...root.querySelectorAll('img,canvas')].some(visible)||[...root.querySelectorAll('*')].some(e=>visible(e)&&getComputedStyle(e).backgroundImage!=='none');if(visual||/重新上传封面|更换封面/.test(text))return true}}return false})()").catch(()=>false);if(coverReady)break}if(!coverReady){const coverDiagnostics=await wc.executeJavaScript("(()=>JSON.stringify({buttons:[...document.querySelectorAll('button,[role=button]')].filter(e=>e.offsetParent!==null).map(e=>(e.textContent||'').trim()).filter(Boolean).slice(-50),dialogs:[...document.querySelectorAll('[role=dialog],[class*=modal],[class*=dialog]')].filter(e=>e.offsetParent!==null).map(e=>(e.textContent||'').trim()).filter(Boolean).slice(-20),body:(document.body?.innerText||'').slice(-5000)}))()").catch(()=>"");throw new Error('头条封面上传后未生成有效预览'+(coverDiagnostics?'：'+coverDiagnostics:''))}}else{const nearbyCover=account.platform==='douyin'?await this.findFileInputNearText(wc,'\u9009\u62e9\u5c01\u9762'):undefined;const refreshed=await this.getFileInputs(wc);const coverInput=nearbyCover||refreshed.find(x=>/image/i.test(x.accept)&&x.nodeId!==videoNodeId);if(coverInput)await this.setFileInput(wc,coverInput.nodeId,[draft.coverPath]);else{const uploaded=await this.uploadFileByActions(wc,draft.coverPath,'image',['选择封面','上传封面','本地上传','点击上传'],[videoNodeId],30000);if(!uploaded)throw new Error(account.platform==='douyin'?'未找到抖音封面上传控件':'未找到封面上传控件')}if(account.platform==='douyin'){await sleep(4000);const x=841,y=585;wc.sendInputEvent({type:'mouseMove',x,y});wc.sendInputEvent({type:'mouseDown',x,y,button:'left',clickCount:1});await sleep(120);wc.sendInputEvent({type:'mouseUp',x,y,button:'left',clickCount:1});await sleep(2500)}}}await progress('uploading','视频上传中，等待平台处理');const publishTimeout=account.platform==='douyin'||account.platform==='toutiao'?600000:180000;const ready=account.platform==='xiaohongshu'?await this.waitForXhsPublishReady(wc,publishTimeout):await this.waitForPublishReady(wc,account.platform,publishTimeout);if(!ready){const douyinDiagnostics=account.platform==='douyin'?await wc.executeJavaScript("(()=>JSON.stringify({url:location.href,fields:[...document.querySelectorAll('input,textarea,[contenteditable=true]')].map(e=>{const r=e.getBoundingClientRect();return{tag:e.tagName,type:e.type||'',placeholder:e.getAttribute('placeholder')||'',contenteditable:e.getAttribute('contenteditable'),value:typeof e.value==='string'?e.value:(e.textContent||''),visible:!!e.offsetParent,rect:{x:r.x,y:r.y,width:r.width,height:r.height},html:(e.outerHTML||'').slice(0,800)}}),buttons:[...document.querySelectorAll('button,[role=button]')].map(e=>({text:(e.textContent||'').trim(),disabled:!!e.disabled,ariaDisabled:e.getAttribute('aria-disabled'),visible:!!e.offsetParent})).filter(e=>e.visible).slice(-80),body:(document.body?.innerText||'').slice(0,12000)}))()").catch(error=>'诊断失败：'+String(error)):'';throw new Error(account.platform==='xiaohongshu'?'小红书真实发布按钮持续禁用，请检查视频处理、标题、正文、封面或平台校验提示':'等待视频上传完成超时'+(douyinDiagnostics?'；抖音控件诊断：'+douyinDiagnostics:''))}if(account.platform==='bilibili'||account.platform==='kuaishou'||account.platform==='douyin'){await wc.executeJavaScript("window.scrollTo(0,document.documentElement.scrollHeight);true");await sleep(1200)}await progress('publishing','正在提交发布');let clicked=false,clickDiagnostics='';if(account.platform==='xiaohongshu'){if(!wc.debugger.isAttached())wc.debugger.attach('1.3');await wc.debugger.sendCommand('DOM.enable');const flattened=await wc.debugger.sendCommand('DOM.getFlattenedDocument',{depth:-1,pierce:true});const nodes=flattened.nodes as Array<{nodeId:number,parentId?:number,nodeName:string,nodeValue?:string,attributes?:string[],backendNodeId?:number}>;const byId=new Map(nodes.map(node=>[node.nodeId,node]));const buttonIds=new Set<number>();for(const textNode of nodes.filter(node=>node.nodeName==='#text'&&(node.nodeValue||'').trim()==='发布')){let current:typeof textNode|undefined=textNode;while(current&&current.nodeName!=='BUTTON'&&current.parentId)current=byId.get(current.parentId);if(current?.nodeName==='BUTTON')buttonIds.add(current.nodeId)}const buttonPoints:Array<{nodeId:number;x:number;y:number;width:number;height:number}>=[];for(const nodeId of buttonIds){try{const model=await wc.debugger.sendCommand('DOM.getBoxModel',{nodeId});const quad=(model.model.border||model.model.content) as number[];const xs=[quad[0],quad[2],quad[4],quad[6]],ys=[quad[1],quad[3],quad[5],quad[7]];buttonPoints.push({nodeId,x:xs.reduce((sum,value)=>sum+value,0)/4,y:ys.reduce((sum,value)=>sum+value,0)/4,width:Math.max(...xs)-Math.min(...xs),height:Math.max(...ys)-Math.min(...ys)})}catch{}}buttonPoints.sort((a,b)=>b.y-a.y||b.x-a.x);const chosenButton=buttonPoints.find(candidate=>candidate.width>40&&candidate.height>20);let point:({x:number;y:number})|undefined=chosenButton;if(!point)point=await wc.executeJavaScript("(()=>{const target=document.querySelector('xhs-publish-btn');if(!target)return null;const rect=target.getBoundingClientRect();return{x:Math.round(rect.left+rect.width*0.61),y:Math.round(rect.top+rect.height/2)}})()");clickDiagnostics=JSON.stringify({buttonPoints,chosenPoint:point,activation:chosenButton?'focus-enter':'mouse-fallback'});if(chosenButton){const resolved=await wc.debugger.sendCommand('DOM.resolveNode',{nodeId:chosenButton.nodeId});const objectId=resolved.object?.objectId;if(!objectId)throw new Error('无法解析小红书发布按钮对象');const invoked=await wc.debugger.sendCommand('Runtime.callFunctionOn',{objectId,functionDeclaration:'function(){const before={disabled:!!this.disabled,ariaDisabled:this.getAttribute?.(\"aria-disabled\"),outerHTML:(this.outerHTML||\"\").slice(0,500)};this.click();return before}',returnByValue:true,awaitPromise:true});clickDiagnostics=JSON.stringify({buttonPoints,chosenPoint:point,activation:'runtime-click',button:invoked.result?.value});clicked=true}else if(point&&Number.isFinite(point.x)&&Number.isFinite(point.y)){await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mouseMoved',x:point.x,y:point.y});await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mousePressed',x:point.x,y:point.y,button:'left',buttons:1,clickCount:1});await sleep(120);await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mouseReleased',x:point.x,y:point.y,button:'left',buttons:0,clickCount:1});clicked=true}}else if(account.platform==='douyin'){clicked=await wc.executeJavaScript("(()=>{const buttons=[...document.querySelectorAll('button')].filter(button=>(button.textContent||'').trim()==='发布'&&!button.disabled&&button.getAttribute('aria-disabled')!=='true');const target=buttons.at(-1);if(!target)return false;target.scrollIntoView({block:'center'});target.focus();target.click();return true})()");clickDiagnostics=await wc.executeJavaScript("(()=>JSON.stringify({url:location.href,publishButtons:[...document.querySelectorAll('button')].filter(button=>(button.textContent||'').trim()==='发布').map(button=>({disabled:!!button.disabled,ariaDisabled:button.getAttribute('aria-disabled'),visible:!!button.offsetParent,html:(button.outerHTML||'').slice(0,800)}))}))()").catch(error=>String(error))}else if(account.platform==='bilibili'){await sleep(15000);clickDiagnostics=await wc.executeJavaScript("(()=>JSON.stringify({url:location.href,textareas:[...document.querySelectorAll('textarea')].map(e=>({visible:!!e.offsetParent,value:e.value,placeholder:e.placeholder})),buttons:[...document.querySelectorAll('button')].filter(e=>e.offsetParent!==null).map(e=>({text:(e.textContent||'').trim(),disabled:!!e.disabled,className:String(e.className)})).slice(-40),body:(document.body?.innerText||'').slice(0,16000)}))()").catch(error=>String(error));clicked=await this.clickButtonByText(wc,['\u7acb\u5373\u6295\u7a3f'],true);if(clicked){await sleep(1200);await this.clickButtonByText(wc,['\u786e\u5b9a','\u786e\u8ba4\u6295\u7a3f','\u7ee7\u7eed\u6295\u7a3f'],true)}}else{clicked=await this.clickButtonByText(wc,['发布','立即发布','确认发布','投稿','立即投稿','提交发布','发表'],true)} if(!clicked)throw new Error('没有找到可用的发布按钮');if(account.platform==='kuaishou'||account.platform==='xiaohongshu'){for(let attempt=0;attempt<8;attempt++){await sleep(750);const confirmed=await this.clickButtonByText(wc,['确认发布','确认','继续发布'],true);if(confirmed)break}}const result=await this.waitForResult(wc,account.platform,account.platform==='xiaohongshu'?15000:account.platform==='bilibili'?30000:account.platform==='douyin'?600000:account.platform==='toutiao'?30000:120000);if(result.challenge)return{status:'manual_required' as const,message:result.message};const verified=await this.verifyPublishedTitle(wc,account.platform,draft.title,180000);if(verified.ok)return{status:'success' as const,message:platformMap[account.platform].name+'作品管理已找到目标标题，发布成功：'+verified.url};if(result.success){const evidence=await this.captureFailure(wc,account.platform).catch(()=>undefined);return{status:'failed' as const,message:(verified.reason||'平台后台未找到目标作品')+(evidence?'；现场截图：'+evidence:'')}}const diagnostics=account.platform==='xiaohongshu'?await wc.executeJavaScript("(()=>JSON.stringify({viewport:{width:innerWidth,height:innerHeight},customButtons:[...document.querySelectorAll('xhs-publish-btn')].map(e=>{const r=e.getBoundingClientRect();return{rect:{left:r.left,top:r.top,width:r.width,height:r.height},attributes:[...e.attributes].map(a=>[a.name,a.value]),shadowText:e.shadowRoot?.textContent||'',shadowHtml:(e.shadowRoot?.innerHTML||'').slice(0,1200)}}),publishElements:[...document.querySelectorAll('button,[role=button],div,span')].filter(e=>(e.textContent||'').trim()==='发布').map(e=>{const r=e.getBoundingClientRect(),style=getComputedStyle(e);return{tag:e.tagName,className:String(e.className),disabled:!!e.disabled,ariaDisabled:e.getAttribute('aria-disabled'),pointerEvents:style.pointerEvents,visible:!!e.offsetParent,rect:{left:r.left,top:r.top,width:r.width,height:r.height},parent:e.parentElement?{tag:e.parentElement.tagName,className:String(e.parentElement.className)}:null}}),bottomHit:[...document.elementsFromPoint(innerWidth*.535,innerHeight-46)].slice(0,8).map(e=>({tag:e.tagName,className:String(e.className),text:(e.textContent||'').trim().slice(0,80)})),alerts:[...document.querySelectorAll('[role=alert],.error,.error-message,.ant-message,.toast')].filter(e=>e.offsetParent!==null).map(e=>(e.textContent||'').trim()).filter(Boolean)}))()").catch(error=>'诊断失败：'+String(error)):'';const genericDiagnostics=await wc.executeJavaScript("(()=>JSON.stringify({alerts:[...document.querySelectorAll('[role=alert],.error,.error-message,[class*=error],[class*=tip],[class*=message]')].filter(e=>e.offsetParent!==null).map(e=>(e.textContent||'').trim()).filter(Boolean).slice(-30),body:(document.body?.innerText||'').slice(-4000)}))()").catch(()=>"");const unconfirmedEvidence=await this.captureFailure(wc,account.platform).catch(()=>undefined);return{status:'manual_required' as const,message:'已点击发布但平台未确认结果；当前页面：'+wc.getURL()+(clickDiagnostics?'；点击诊断：'+clickDiagnostics:'')+(diagnostics?'；页面诊断：'+diagnostics:'')+(unconfirmedEvidence?'；现场截图：'+unconfirmedEvidence:'')} }catch(error){const reason=error instanceof Error?error.message:String(error);const evidence=await this.captureFailure(wc,account.platform).catch(()=>undefined);return{status:'failed' as const,message:reason+(evidence?'；现场截图：'+evidence:'')}}finally{if(!automationWindow.isDestroyed())automationWindow.destroy()}}private async waitForXhsPublishReady(wc:Electron.WebContents,timeout:number){if(!wc.debugger.isAttached())wc.debugger.attach('1.3');await wc.debugger.sendCommand('DOM.enable');const startedAt=Date.now();while(Date.now()-startedAt<timeout){const flattened=await wc.debugger.sendCommand('DOM.getFlattenedDocument',{depth:-1,pierce:true});const nodes=flattened.nodes as Array<{nodeId:number,parentId?:number,nodeName:string,nodeValue?:string,attributes?:string[],backendNodeId?:number}>;const byId=new Map(nodes.map(node=>[node.nodeId,node]));for(const textNode of nodes.filter(node=>node.nodeName==='#text'&&(node.nodeValue||'').trim()==='发布')){let current:typeof textNode|undefined=textNode;while(current&&current.nodeName!=='BUTTON'&&current.parentId)current=byId.get(current.parentId);if(current?.nodeName==='BUTTON'){const attributes=current.attributes||[];const attributeMap=new Map<string,string>();for(let index=0;index<attributes.length;index+=2)attributeMap.set(attributes[index],attributes[index+1]||'');if(!attributeMap.has('disabled')&&attributeMap.get('aria-disabled')!=='true')return true}}await sleep(2000)}return false}private async captureFailure(wc:Electron.WebContents,platform:Account['platform']){const dir=path.join(app.getPath('userData'),'task-artifacts');await fs.mkdir(dir,{recursive:true});const file=path.join(dir,platform+'-'+Date.now()+'.png');await fs.writeFile(file,await wc.capturePage().then(image=>image.toPNG()));return file}private async waitForEditor(wc:Electron.WebContents,platform:Account['platform'],timeout:number){const start=Date.now();while(Date.now()-start<timeout){const url=wc.getURL();if(platform==='douyin'&&/creator-micro\/content\/(publish|post\/video)/.test(url)){const ready=await wc.executeJavaScript("(()=>{const visible=e=>!!e&&e.offsetParent!==null;const title=[...document.querySelectorAll('input')].find(e=>visible(e)&&/标题|作品名称/.test(e.placeholder||''));const editor=[...document.querySelectorAll('[contenteditable=true]')].find(visible);return!!title&&!!editor})()");if(ready)return true}if(platform==='xiaohongshu'){const ready=await wc.executeJavaScript("!!document.querySelector('input[placeholder*=标题],input[placeholder*=填写标题],input.upload-input')");if(ready)return true}if(platform==='kuaishou'){const ready=await wc.executeJavaScript("(()=>{const text=document.body?.innerText||'';return /描述|封面设置|发布设置/.test(text)&&!!document.querySelector('[contenteditable=true],textarea,input')})()");if(ready)return true}if(platform==='toutiao'){const ready=await wc.executeJavaScript("(()=>{const text=document.body?.innerText||'';const fields=[...document.querySelectorAll('input,textarea')].filter(e=>e.offsetParent!==null);const cover=/封面|上传封面/.test(text);const settings=/发布设置|发布视频/.test(text);return fields.length>0&&(settings||cover)})()");if(ready)return true}if(platform==='weixin'){const ready=await wc.executeJavaScript("(()=>{const text=document.body?.innerText||'';return /发表视频|视频描述|声明原创/.test(text)&&!!document.querySelector('input[type=file],[contenteditable=true],textarea')})()");if(ready)return true}if(!['douyin','xiaohongshu','kuaishou','weixin'].includes(platform)){const ready=await wc.executeJavaScript("!!document.querySelector('textarea,[contenteditable=true],input[placeholder*=标题]')");if(ready)return true}await sleep(1500)}return false}private async findFileInputNearText(wc:Electron.WebContents,text:string){if(!wc.debugger.isAttached())wc.debugger.attach('1.3');await wc.debugger.sendCommand('DOM.enable');const flattened=await wc.debugger.sendCommand('DOM.getFlattenedDocument',{depth:-1,pierce:true});const nodes=flattened.nodes as Array<{nodeId:number;backendNodeId?:number;parentId?:number;nodeName:string;nodeValue?:string;attributes?:string[]}>;const byId=new Map(nodes.map(node=>[node.nodeId,node]));const children=new Map<number,typeof nodes>();for(const node of nodes){if(!node.parentId)continue;const list=children.get(node.parentId)||[];list.push(node);children.set(node.parentId,list)}const readInput=(node:typeof nodes[number])=>{if(node.nodeName!=='INPUT')return undefined;const attrs=node.attributes||[];const values=new Map<string,string>();for(let index=0;index<attrs.length;index+=2)values.set(attrs[index],attrs[index+1]||'');const type=(values.get('type')||'').toLowerCase(),accept=values.get('accept')||'';return type==='file'||accept?{nodeId:node.backendNodeId||node.nodeId,accept}:undefined};const descendantInputs=(root:number)=>{const queue=(children.get(root)||[]).map(node=>({node,depth:1}));const found:Array<{input:{nodeId:number;accept:string};depth:number}>=[];while(queue.length){const current=queue.shift()!;const input=readInput(current.node);if(input)found.push({input,depth:current.depth});for(const child of children.get(current.node.nodeId)||[])queue.push({node:child,depth:current.depth+1})}return found};const wantsImage=/封面|图片/.test(text),wantsVideo=/视频/.test(text)&&!wantsImage;const candidates=new Map<number,{input:{nodeId:number;accept:string};score:number;y:number;area:number}>();for(const textNode of nodes.filter(node=>node.nodeName==='#text'&&(node.nodeValue||'').trim()===text)){let textY=-1,textArea=Number.MAX_SAFE_INTEGER;try{const parentId=textNode.parentId||textNode.nodeId;const model=await wc.debugger.sendCommand('DOM.getBoxModel',{nodeId:parentId});const quad=(model.model.border||model.model.content) as number[];const xs=[quad[0],quad[2],quad[4],quad[6]],ys=[quad[1],quad[3],quad[5],quad[7]];textY=ys.reduce((sum,value)=>sum+value,0)/4;textArea=(Math.max(...xs)-Math.min(...xs))*(Math.max(...ys)-Math.min(...ys))}catch{continue}let current=textNode.parentId?byId.get(textNode.parentId):undefined;for(let ancestorDepth=0;current&&ancestorDepth<12;ancestorDepth++,current=current.parentId?byId.get(current.parentId):undefined){for(const found of descendantInputs(current.nodeId)){const accept=found.input.accept;const typePenalty=wantsImage?(/image/i.test(accept)?0:10000):wantsVideo?(/video/i.test(accept)?0:/image/i.test(accept)?10000:50):0;const score=ancestorDepth*100+found.depth+typePenalty;const previous=candidates.get(found.input.nodeId);if(!previous||score<previous.score)candidates.set(found.input.nodeId,{input:found.input,score,y:textY,area:textArea})}}}return[...candidates.values()].sort((a,b)=>a.score-b.score||b.y-a.y||a.area-b.area)[0]?.input}private async clickSmallestByText(wc:Electron.WebContents,text:string){if(!wc.debugger.isAttached())wc.debugger.attach('1.3');await wc.debugger.sendCommand('DOM.enable');const flattened=await wc.debugger.sendCommand('DOM.getFlattenedDocument',{depth:-1,pierce:true});const nodes=flattened.nodes as Array<{nodeId:number;parentId?:number;nodeName:string;nodeValue?:string;attributes?:string[]}>;const byId=new Map(nodes.map(node=>[node.nodeId,node]));const candidates:Array<{x:number;y:number;area:number;rank:number}>=[];for(const textNode of nodes.filter(node=>node.nodeName==='#text'&&(node.nodeValue||'').trim()===text)){let current:typeof textNode|undefined=textNode.parentId?byId.get(textNode.parentId):undefined;let hidden=false;let visibilityNode=current;for(let depth=0;visibilityNode&&depth<8;depth++,visibilityNode=visibilityNode.parentId?byId.get(visibilityNode.parentId):undefined){const attrs=(visibilityNode.attributes||[]).join(' ');if(/display\s*:\s*none|visibility\s*:\s*hidden/i.test(attrs))hidden=true}for(let depth=0;current&&!hidden&&depth<6;depth++,current=current.parentId?byId.get(current.parentId):undefined){try{const model=await wc.debugger.sendCommand('DOM.getBoxModel',{nodeId:current.nodeId});const quad=(model.model.border||model.model.content) as number[];const xs=[quad[0],quad[2],quad[4],quad[6]],ys=[quad[1],quad[3],quad[5],quad[7]],width=Math.max(...xs)-Math.min(...xs),height=Math.max(...ys)-Math.min(...ys),area=width*height,attrs=(current.attributes||[]).join(' ');if(width>3&&height>3)candidates.push({x:xs.reduce((sum,value)=>sum+value,0)/4,y:ys.reduce((sum,value)=>sum+value,0)/4,area,rank:current.nodeName==='LI'&&/bcc-option/i.test(attrs)?0:current.nodeName==='ARTICLE'?1:2})}catch{}}}candidates.sort((a,b)=>a.rank-b.rank||a.area-b.area);const target=candidates[0];if(!target)return false;const x=target.x,y=target.y;await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mouseMoved',x,y});await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mousePressed',x,y,button:'left',buttons:1,clickCount:1});await sleep(120);await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mouseReleased',x,y,button:'left',buttons:0,clickCount:1});return true}private async clickButtonByText(wc:Electron.WebContents,texts:string[],preferBottom=true,trustedOnly=false){if(!wc.debugger.isAttached())wc.debugger.attach('1.3');const script="((texts,preferBottom)=>{const visible=e=>!!e&&e.offsetParent!==null;const candidates=[...document.querySelectorAll('button,[role=button],a,div,span')].filter(e=>visible(e)&&texts.includes((e.textContent||'').trim())&&!e.disabled&&e.getAttribute('aria-disabled')!=='true');candidates.sort((a,b)=>{const ar=a.getBoundingClientRect(),br=b.getBoundingClientRect();return(preferBottom?br.top-ar.top:ar.top-br.top)||((ar.width*ar.height)-(br.width*br.height))});const target=candidates[0];if(!target)return false;target.scrollIntoView({block:'center',inline:'center'});target.focus?.();target.click();return true})("+JSON.stringify(texts)+","+JSON.stringify(preferBottom)+")";const clicked=trustedOnly?false:await wc.executeJavaScript(script).catch(()=>false);if(clicked)return true;await wc.debugger.sendCommand('DOM.enable');const flattened=await wc.debugger.sendCommand('DOM.getFlattenedDocument',{depth:-1,pierce:true});const nodes=flattened.nodes as Array<{nodeId:number;parentId?:number;nodeName:string;nodeValue?:string}>;const byId=new Map(nodes.map(node=>[node.nodeId,node]));const candidates:Array<{x:number;y:number;area:number}>=[];for(const textNode of nodes.filter(node=>node.nodeName==='#text'&&texts.includes((node.nodeValue||'').trim()))){let current=textNode.parentId?byId.get(textNode.parentId):undefined;for(let depth=0;current&&depth<6;depth++,current=current.parentId?byId.get(current.parentId):undefined){try{const model=await wc.debugger.sendCommand('DOM.getBoxModel',{nodeId:current.nodeId});const quad=(model.model.border||model.model.content) as number[];const xs=[quad[0],quad[2],quad[4],quad[6]],ys=[quad[1],quad[3],quad[5],quad[7]],width=Math.max(...xs)-Math.min(...xs),height=Math.max(...ys)-Math.min(...ys);if(width>3&&height>3&&width<700&&height<180)candidates.push({x:xs.reduce((sum,value)=>sum+value,0)/4,y:ys.reduce((sum,value)=>sum+value,0)/4,area:width*height})}catch{}}}candidates.sort((a,b)=>preferBottom?(b.y-a.y||a.area-b.area):(a.y-b.y||a.area-b.area));const target=candidates[0];if(!target)return false;await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mouseMoved',x:target.x,y:target.y});await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mousePressed',x:target.x,y:target.y,button:'left',buttons:1,clickCount:1});await sleep(120);await wc.debugger.sendCommand('Input.dispatchMouseEvent',{type:'mouseReleased',x:target.x,y:target.y,button:'left',buttons:0,clickCount:1});return true}private async chooseFileFromButton(wc:Electron.WebContents,selector:string|string[],file:string){if(!wc.debugger.isAttached())wc.debugger.attach('1.3');const labels=Array.isArray(selector)?selector:selector==='toutiao-cover'?['本地上传','上传图片','点击上传','上传封面']:selector==='douyin'?['上传视频','点击上传','选择视频','上传']:['上传视频','点击上传','选择文件'];const imageMode=(Array.isArray(selector)&&selector.some(value=>/封面|图片/.test(value)))||selector==='toutiao-cover';const before=(await this.getFileInputs(wc).catch(()=>[])).map(input=>input.nodeId);let chooserNodeId:number|undefined;const listener=(_event:Electron.Event,method:string,params:any)=>{if(method==='Page.fileChooserOpened')chooserNodeId=params.backendNodeId};wc.debugger.on('message',listener);await wc.debugger.sendCommand('Page.setInterceptFileChooserDialog',{enabled:true});try{const clicked=await this.clickButtonByText(wc,labels,true,true);if(!clicked)throw new Error(labels.join('/')+'上传按钮不存在');const start=Date.now();while(Date.now()-start<30000){if(chooserNodeId){await wc.debugger.sendCommand('DOM.setFileInputFiles',{backendNodeId:chooserNodeId,files:[file]});return}const inputs=await this.getFileInputs(wc).catch(()=>[]);const candidates=inputs.filter(input=>imageMode?/image/i.test(input.accept):/video/i.test(input.accept)||!/image/i.test(input.accept));const fresh=candidates.filter(input=>!before.includes(input.nodeId));const target=fresh.at(-1)||candidates.at(-1);if(target&&Date.now()-start>800){await this.setFileInput(wc,target.nodeId,[file]);return}await sleep(400)}throw new Error(labels.join('/')+'未产生可用文件控件')}finally{wc.debugger.removeListener('message',listener);await wc.debugger.sendCommand('Page.setInterceptFileChooserDialog',{enabled:false}).catch(()=>undefined)}}private async waitForFileInputNearTexts(wc:Electron.WebContents,texts:string[],timeout:number){const start=Date.now();while(Date.now()-start<timeout){for(const text of texts){try{const input=await this.findFileInputNearText(wc,text);if(input)return input}catch{}}await sleep(500)}return undefined}private async waitAndClickButton(wc:Electron.WebContents,texts:string[],timeout:number,preferBottom=true){const start=Date.now();while(Date.now()-start<timeout){try{if(await this.clickButtonByText(wc,texts,preferBottom))return true}catch{}await sleep(500)}return false}private async waitForMatchingFileInput(wc:Electron.WebContents,kind:'video'|'image',timeout:number,excluded:number[]=[]){const start=Date.now();while(Date.now()-start<timeout){try{const inputs=await this.getFileInputs(wc);const candidates=inputs.filter(input=>!excluded.includes(input.nodeId)&&(kind==='image'?/image/i.test(input.accept):/video/i.test(input.accept)||!/image/i.test(input.accept)));if(candidates.length)return candidates[0]}catch{}await sleep(500)}return undefined}private async uploadFileByActions(wc:Electron.WebContents,file:string,kind:'video'|'image',actions:string[],excluded:number[]=[],timeout=30000){const before=(await this.getFileInputs(wc).catch(()=>[])).map(input=>input.nodeId);const clicked=await this.waitAndClickButton(wc,actions,Math.min(timeout,15000),false);if(!clicked)return false;const fresh=await this.waitForMatchingFileInput(wc,kind,Math.min(timeout,15000),[...excluded,...before]);if(fresh){await this.setFileInput(wc,fresh.nodeId,[file]);return true}try{await this.chooseFileFromButton(wc,actions,file);return true}catch{}const existing=await this.waitForMatchingFileInput(wc,kind,Math.min(timeout,5000),excluded);if(existing){await this.setFileInput(wc,existing.nodeId,[file]);return true}return false}private async waitForFileInputs(wc:Electron.WebContents,timeout:number){const start=Date.now();while(Date.now()-start<timeout){try{const inputs=await this.getFileInputs(wc);if(inputs.length)return inputs}catch{}await sleep(1500)}return[]}private async getFileInputs(wc:Electron.WebContents){if(!wc.debugger.isAttached())wc.debugger.attach('1.3');await wc.debugger.sendCommand('DOM.enable');const flattened=await wc.debugger.sendCommand('DOM.getFlattenedDocument',{depth:-1,pierce:true});const result:{nodeId:number;accept:string}[]=[];for(const node of flattened.nodes as Array<{nodeId:number;nodeName:string;attributes?:string[];backendNodeId?:number}>){if(node.nodeName!=='INPUT')continue;const attributes=node.attributes||[];const values=new Map<string,string>();for(let index=0;index<attributes.length;index+=2)values.set(attributes[index],attributes[index+1]||'');const type=(values.get('type')||'').toLowerCase(),accept=values.get('accept')||'';if(type==='file'||accept)result.push({nodeId:node.backendNodeId||node.nodeId,accept})}return result}private async setFileInput(wc:Electron.WebContents,nodeId:number,files:string[]){if(!wc.debugger.isAttached())wc.debugger.attach('1.3');await wc.debugger.sendCommand('DOM.setFileInputFiles',{backendNodeId:nodeId,files})}private async fillContent(wc:Electron.WebContents,draft:PublishDraft,platform:Account['platform']){if(platform==='bilibili'||platform==='kuaishou')await sleep(3000);const titleLimit=platform==='xiaohongshu'?20:(platform==='douyin'||platform==='toutiao')?30:undefined;const title=titleLimit?Array.from(draft.title).slice(0,titleLimit).join(''):draft.title;const payload=JSON.stringify({title,description:draft.description,tags:draft.topics,platform});await wc.executeJavaScript("(()=>{const data="+payload+";const visible=e=>e&&e.offsetParent!==null;const set=(el,value)=>{if(!el)return false;el.focus();if(el.isContentEditable){el.textContent=value;el.dispatchEvent(new InputEvent('input',{bubbles:true,inputType:'insertText',data:value}))}else{const setter=Object.getOwnPropertyDescriptor(Object.getPrototypeOf(el),'value')?.set;setter?setter.call(el,value):el.value=value;el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}))}return true};const fields=[...document.querySelectorAll('input,textarea,[contenteditable=true]')].filter(visible);const title=fields.find(e=>/标题|作品名称|视频名称/.test(e.getAttribute('placeholder')||''))||fields.find(e=>e.tagName==='INPUT'&&e.type==='text');set(title,data.title);const description=data.platform==='bilibili'?fields.find(e=>e.tagName==='TEXTAREA'&&/更全面|相关信息|视频/.test(e.getAttribute('placeholder')||''))||fields.find(e=>e.tagName==='TEXTAREA'):fields.find(e=>/简介|描述|正文|内容/.test(e.getAttribute('placeholder')||''))||fields.find(e=>e.tagName==='TEXTAREA')||fields.find(e=>e.isContentEditable&&e!==title);set(description,data.description);const tagField=fields.find(e=>/话题|标签/.test(e.getAttribute('placeholder')||''));if(tagField&&data.tags.length)set(tagField,data.tags.map(t=>'#'+t).join(' '));return{title:typeof title?.value==='string'?title.value:title?.textContent||'',description:typeof description?.value==='string'?description.value:description?.textContent||''}})()")}private async waitForPublishReady(wc:Electron.WebContents,platform:Account['platform'],timeout:number){const start=Date.now();while(Date.now()-start<timeout){const state=await wc.executeJavaScript("(()=>{const visible=e=>!!e&&e.offsetParent!==null;const text=document.body?.innerText||'';const buttons=[...document.querySelectorAll('button,[role=button],div,span')].filter(visible);const challenge=/扫码登录|验证码|安全验证|重新登录/.test(text);const uploading=/上传中|正在上传|转码中|上传进度/.test(text);const publish=buttons.some(b=>/^(发布|立即发布|确认发布|投稿|立即投稿|提交发布|发表)$/.test((b.textContent||'').trim())&&!b.disabled);const uploaded=/上传成功|上传完成|视频预览|智能封面|封面候选|视频文件|重新上传|检测为高清视频/.test(text)||!!document.querySelector('video');return{challenge,uploading,publish,uploaded,text,url:location.href}})()");if(state.challenge)throw new Error('平台要求登录或安全验证');if(platform==='xiaohongshu'&&state.publish&&/视频文件|重新上传|检测为高清视频/.test(state.text))return true;if(platform==='douyin'&&state.publish&&state.uploaded)return true;if(state.publish&&state.uploaded&&!state.uploading)return true;if(platform==='toutiao'&&state.publish&&!state.uploading&&/上传成功/.test(state.text))return true;await sleep(1500)}return false}private async verifyPublishedTitle(wc:Electron.WebContents,platform:Account['platform'],title:string,timeout:number){const managerUrls:Partial<Record<Account['platform'],string>>={douyin:'https://creator.douyin.com/creator-micro/content/manage',kuaishou:'https://cp.kuaishou.com/article/manage/video',xiaohongshu:'https://creator.xiaohongshu.com/',toutiao:'https://mp.toutiao.com/profile_v4/manage/content/all',bilibili:'https://member.bilibili.com/platform/upload-manager/article',weixin:'https://channels.weixin.qq.com/platform/post/list'};const managerUrl=managerUrls[platform];if(!managerUrl)return{ok:false,url:wc.getURL(),reason:platformMap[platform].name+'尚未配置后台标题验真'};const limit=platform==='xiaohongshu'?20:(platform==='douyin'||platform==='toutiao')?30:undefined;const platformTitle=limit?Array.from(title).slice(0,limit).join(''):title;const normalize=(value:string)=>value.normalize('NFKC').toLowerCase().replace(/[^\p{L}\p{N}]+/gu,'');const normalizedTitle=normalize(platformTitle);const expected=[normalizedTitle,normalizedTitle.slice(0,24),normalizedTitle.slice(0,20),normalizedTitle.slice(0,16),normalizedTitle.slice(0,12)].filter(value=>value.length>=8);const start=Date.now();let lastUrl='';while(Date.now()-start<timeout){await wc.loadURL(managerUrl).catch(error=>{if(!/ERR_ABORTED/.test(String(error)))throw error});await sleep(7000);if(platform==='xiaohongshu'){const opened=await this.clickSmallestByText(wc,'笔记管理');if(!opened){const href=await wc.executeJavaScript("(()=>{const link=[...document.querySelectorAll('a')].find(e=>(e.textContent||'').trim()==='笔记管理');return link?.href||''})()").catch(()=>"");if(href)await wc.loadURL(href)}await sleep(5000)}if(platform==='kuaishou'||platform==='douyin'){await this.clickSmallestByText(wc,'已发布');await sleep(4000)}const pollUntil=Math.min(start+timeout,Date.now()+45000);while(Date.now()<pollUntil){const snapshot=await wc.executeJavaScript("(()=>({url:location.href,text:(document.body?.innerText||'').slice(0,120000),login:/扫码登录|手机号登录|验证码登录|短信登录|密码登录|立即登录/.test(document.body?.innerText||'')||location.pathname.includes('/login')||location.pathname.includes('/auth')}))()").catch(()=>({url:wc.getURL(),text:'',login:false}));lastUrl=snapshot.url;if(snapshot.login)return{ok:false,url:snapshot.url,reason:platformMap[platform].name+'作品管理要求重新登录'};const rawExpected=[platformTitle,Array.from(platformTitle).slice(0,24).join(''),Array.from(platformTitle).slice(0,20).join(''),Array.from(platformTitle).slice(0,16).join(''),Array.from(platformTitle).slice(0,12).join('')].filter(value=>value.length>=8);const normalizedText=normalize(snapshot.text);if(rawExpected.some(value=>snapshot.text.includes(value))||expected.some(value=>normalizedText.includes(value)))return{ok:true,url:snapshot.url};await sleep(3000)}}return{ok:false,url:lastUrl||wc.getURL(),reason:platformMap[platform].name+'作品管理未找到目标标题'}}private async waitForResult(wc:Electron.WebContents,platform:Account['platform'],timeout:number){const patterns:Partial<Record<Account['platform'],RegExp>>={douyin:/creator-micro\/content\/manage/,kuaishou:/article\/manage\/video/,xiaohongshu:/publish\/success/,weixin:/platform\/post\/list|\/post\/list/,toutiao:/profile_v4\/manage\/content(?:\/all)?/,bilibili:/platform\/upload\/video\/(success|manage)/};const start=Date.now();let challengeSeen=false,challengeEvidence='',lastChallenge='',challengeRequested=false;while(Date.now()-start<timeout){const url=wc.getURL();if(patterns[platform]?.test(url))return{success:true,challenge:false,message:'已跳转到平台作品管理页，发布成功：'+url};const state=await wc.executeJavaScript("(()=>{const text=(document.body?.innerText||'').slice(-12000);const success=/\u53d1\u5e03\u6210\u529f|\u6295\u7a3f\u6210\u529f|\u7a3f\u4ef6\u6295\u9012\u6210\u529f|\u53d1\u8868\u6210\u529f|\u4f5c\u54c1\u53d1\u5e03\u6210\u529f|\u63d0\u4ea4\u6210\u529f|\u5df2\u53d1\u5e03/.test(text);const visible=e=>!!e&&e.offsetParent!==null;const challengeTexts=[...document.querySelectorAll('div,span,p,h1,h2,h3,button')].filter(visible).map(e=>(e.textContent||'').trim()).filter(value=>value&&value.length<240&&/验证码|安全验证|扫码验证|手机验证/.test(value));const challengeText=challengeTexts.sort((a,b)=>a.length-b.length)[0]||'';const challenge=!!challengeText;const failed=/发布失败|投稿失败|上传失败|提交失败/.test(text);return{success,challenge,challengeText,failed,text:text.slice(-500)}})()");if(state.challenge){challengeSeen=true;lastChallenge=state.challengeText||'';if(!challengeEvidence)challengeEvidence=await this.captureFailure(wc,platform).catch(()=>"");if(!challengeRequested){challengeRequested=await wc.executeJavaScript("(()=>{const button=[...document.querySelectorAll('button,div,span')].find(e=>e.offsetParent!==null&&(e.textContent||'').trim()==='获取验证码');if(!button)return false;(button.closest('button')||button).click();return true})()").catch(()=>false)}const owner=BrowserWindow.fromWebContents(wc);if(owner&&!owner.isDestroyed()){owner.setOpacity(1);owner.setAlwaysOnTop(true,'screen-saver');owner.show();owner.focus()}await sleep(2000);continue}if(state.failed)return{success:false,challenge:true,message:'平台返回发布失败：'+state.text};if(state.success&&(platform==='xiaohongshu'||platform==='bilibili'))return{success:true,challenge:false,message:'平台页面已确认发布成功'};await sleep(2000)}return{success:false,challenge:challengeSeen,message:challengeSeen?'平台安全验证未在等待时间内完成：'+lastChallenge+(challengeEvidence?'；现场截图：'+challengeEvidence:''):''}}private async getLoginState(account:Account,wc:Electron.WebContents){const url=wc.getURL();const host=(()=>{try{return new URL(url).hostname}catch{return''}})();if(/login|passport|signin|auth/i.test(url))return{loggedIn:false,name:''};try{const result=await wc.executeJavaScript("(()=>{const visible=e=>!!e&&e.offsetParent!==null;const text=(document.body?.innerText||'').slice(0,12000);const selectors=['[class*=user-name]','[class*=username]','[class*=nickname]','[class*=account-name]','[class*=userName]','[class*=nickName]'];let name='';for(const selector of selectors){const node=[...document.querySelectorAll(selector)].find(visible);const value=node?.textContent?.trim();if(value&&value.length>1&&value.length<40){name=value;break}}const visibleLogin=[...document.querySelectorAll('main#login-form,.login-box-container,[class*=login-box],img[alt=qrcode]')].some(visible);const loginText=/扫码登录|手机号登录|验证码登录|QQ登录|微信登录|APP扫一扫登录/.test(text);return{name,visibleLogin,loginText,text}})()");let loggedIn=false;if(account.platform==='kuaishou')loggedIn=host==='cp.kuaishou.com'&&!result.visibleLogin;if(account.platform==='douyin')loggedIn=host==='creator.douyin.com'&&!result.visibleLogin&&!result.loginText;if(account.platform==='xiaohongshu')loggedIn=host==='creator.xiaohongshu.com'&&!result.visibleLogin;if(account.platform==='weixin')loggedIn=host==='channels.weixin.qq.com'&&!result.visibleLogin&&!result.loginText&&/发表视频|内容管理|数据概览|动态管理/.test(result.text);if(account.platform==='toutiao')loggedIn=/mp.toutiao.com$/.test(host)&&!result.visibleLogin&&!result.loginText;if(account.platform==='bilibili')loggedIn=host==='member.bilibili.com'&&!result.visibleLogin&&!result.loginText;if(account.platform==='qqmedia')loggedIn=host==='om.qq.com'&&!result.visibleLogin&&!result.loginText&&/内容管理|发布|数据/.test(result.text);return{loggedIn,name:result.name}}catch{return{loggedIn:false,name:''}}}private async inspectAccount(account:Account,view:WebContentsView){const state=await this.getLoginState(account,view.webContents);if(!state.loggedIn){await this.updateAccount(account.id,{loginStatus:'logged_out'});return}await this.updateAccount(account.id,{loginStatus:'logged_in',...(state.name?{name:state.name}:{})})}hide(){if(!this.active)return;const v=this.views.get(this.active);if(v&&this.win.contentView.children.includes(v))this.win.contentView.removeChildView(v);this.active=undefined}close(id:string){const v=this.views.get(id);if(!v)return;if(this.win.contentView.children.includes(v))this.win.contentView.removeChildView(v);v.webContents.close();this.views.delete(id);if(this.active===id)this.active=undefined}setBounds(b:BrowserBounds){this.bounds={x:Math.max(0,Math.round(b.x)),y:Math.max(0,Math.round(b.y)),width:Math.max(100,Math.round(b.width)),height:Math.max(100,Math.round(b.height))};if(this.active)this.views.get(this.active)?.setBounds(this.bounds)}navigate(action:'back'|'forward'|'reload'|'home',account?:Account){if(!this.active)return;const wc=this.views.get(this.active)?.webContents;if(!wc)return;if(action==='back'&&wc.canGoBack())wc.goBack();if(action==='forward'&&wc.canGoForward())wc.goForward();if(action==='reload')wc.reload();if(action==='home'&&account)wc.loadURL(platformMap[account.platform].homeUrl)}remove(id:string){this.close(id)}destroy(){for(const v of this.views.values())v.webContents.close();this.views.clear()}}


